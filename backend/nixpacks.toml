[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "npm start"

[providers]
node = "18"
